<p id="notice"><%= notice %></p>

<p>
  <strong>Goal:</strong>
  <%= @plan.goal %>
</p>

<p>
	<%= link_to 'Edit', edit_plan_path(@plan) %> |
	<%= link_to 'Back', plans_path %>
</p>

<p>
	<%= link_to 'Add new KPI', new_kpi_path(:plan_id=>@plan.id) %>
</p>
